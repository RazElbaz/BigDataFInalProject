<div class="col-lg-6">
    <div class="card bg-dark">
        <div class="card-body">
            <h3 class="row card-title-center"><%= title %></h3>
            <div class="text-center">
                <button id="<%= buttonID %>" type="button" class="row btn btn-primary text-center"><%= button_title %></button>
            </div>
        </div>
    </div>
</div>